# Appendix A - Requirements Traceability Matrix

Status: first-pass Engineering MVP and SRS acceptance evidence artifact

This matrix is the working Appendix A traceability record for SRS requirements
in `docs/OxideDNS-Secondary-SRS-v0.7.md` sections 3 through 6. It is derived
from the SRS, `docs/verification-ledger.md`, `docs/mvp-gap-register.md`, and
the evidence pointers they already cite.

This is not a final release conformance claim. The status column records the
current evidence state for each requirement family, not implementation
completion. `MVP` in the phase column means the SRS-defined ODS-VER-008
acceptance gate, which is broader than the near-term Engineering MVP described
in `docs/implementation-plan.md`. Requirement IDs are kept as exact SRS IDs or
same-prefix ranges so the table can be checked mechanically for identifier
drift.

## Status Values

- **Not Verified**: no accepted evidence is recorded for this family.
- **Partial**: evidence exists, but does not cover the full listed scope.
- **Verified**: evidence covers the listed scope and is suitable for release
  review.
- **Deferred**: verification is intentionally targeted at a later milestone.

## Architectural Invariants

| Requirement ID | Phase / status | Evidence pointer | Remaining acceptance gap |
| --- | --- | --- | --- |
| ODS-INV-001..ODS-INV-009 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/audit-invariants.sh`; `scripts/check.sh`; `docs/verification-ledger.md`; `crates/oxidedns-core/src/dns.rs` tests `dns_update_opcode_gets_notimp_without_zone_mutation` and `concurrent_snapshot_replacement_answers_from_one_zone_version` | Static invariant inspection now covers SRS v0.7 INV-001 through INV-009, including authoritative-only response composition, single-process operation, and no runtime code loading; add read-only root filesystem runtime evidence and panic-free query-path evidence before acceptance review. |

## Functional Requirements

| Requirement ID | Phase / status | Evidence pointer | Remaining acceptance gap |
| --- | --- | --- | --- |
| ODS-FR-CORE-001..ODS-FR-CORE-028 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; `scripts/interop-bind-axfr.sh`; `scripts/interop-nsd-axfr-docker.sh`; `scripts/interop-knot-axfr-docker.sh` | Split broad query and interop evidence into per-requirement release artifacts for parser, header, lookup, referral, wildcard, RRset, TTL, and name-octet behavior. |
| ODS-FR-QRY-001..ODS-FR-QRY-024 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; AXFR interop scripts listed above | Minimal-ANY behavior, CNAME/DNAME edge cases, additional-section composition, compression policy, and query counter evidence still need per-requirement release traceability. |
| ODS-FR-NRESP-001..ODS-FR-NRESP-006 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; primary interop scripts listed in `docs/verification-ledger.md` | Release artifacts must isolate NXDOMAIN, NODATA, empty non-terminal, CNAME/DNAME terminal, SOA TTL, and out-of-zone chain behavior. |
| ODS-FR-URR-001..ODS-FR-URR-009 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh` | Preserve bit-for-bit unknown-type transfer and response evidence as retained release output, including zero-length RDATA and reserved-type rejection. |
| ODS-FR-SPOOF-001..ODS-FR-SPOOF-007 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; AXFR/IXFR interop scripts | Add focused evidence for outbound QID/source validation, discard paths, and warning logs. |
| ODS-FR-AXFR-001..ODS-FR-AXFR-024 | Alpha / Partial | `scripts/interop-bind-axfr.sh`; `scripts/interop-nsd-axfr-docker.sh`; `scripts/interop-knot-axfr-docker.sh`; `docs/implementation-plan.md` | Expand retained release evidence from script-level success into per-requirement AXFR construction, stream validation, error, timeout, concurrency, and publication artifacts. |
| ODS-FR-IXFR-001..ODS-FR-IXFR-019 | MVP / Partial | `scripts/interop-bind-ixfr-refresh.sh`; `scripts/interop-knot-ixfr-refresh-docker.sh`; `scripts/interop-ixfr-notimp-fallback.sh`; `docs/implementation-plan.md`; `docs/mvp-gap-register.md` | Add broader real-primary IXFR behavior matrix where primary support permits it, including fallback and fault evidence mapped by requirement. |
| ODS-FR-NOTIFY-001..ODS-FR-NOTIFY-011 | Alpha / Partial | `scripts/interop-bind-notify-refresh.sh`; `scripts/interop-nsd-notify-refresh-docker.sh`; `scripts/interop-knot-notify-refresh-docker.sh`; `docs/implementation-plan.md` | Release traceability still needs negative NOTIFY cases, authorization/TSIG failure branches, deduplication, and refresh-trigger artifacts separated by requirement. |
| ODS-FR-TSIG-001..ODS-FR-TSIG-017 | Alpha subset; MVP full / Partial | `scripts/interop-bind-tsig-axfr.sh`; `scripts/interop-nsd-tsig-axfr-docker.sh`; `scripts/interop-knot-tsig-axfr-docker.sh`; `docs/implementation-plan.md` | Alpha HMAC-SHA256 AXFR evidence exists; MVP still needs full TSIG verification/error, truncation, algorithm, NOTIFY, and transfer-stream evidence mapped to every TSIG requirement. |
| ODS-FR-XOT-001 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` in-process XoT AXFR tests; `scripts/interop-knot-xot-docker.sh`; `scripts/interop-knot-xot-tsig-docker.sh` | Add explicit TLS 1.2-only, TLS 1.3-only, and mixed-version negotiation artifacts, including proof that TLS 1.3 is preferred when both endpoints support it. |
| ODS-FR-XOT-002 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` in-process XoT TLS client path; Knot XoT interop scripts | Add retained ClientHello/cipher-suite evidence proving only BCP 195 AEAD suites are offered and no prohibited suites are accepted. |
| ODS-FR-XOT-003 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` in-process XoT transfer tests; `scripts/interop-knot-xot-docker.sh`; `scripts/interop-knot-xot-tsig-docker.sh`; `config/oxidedns.example.toml` | Current evidence covers configured non-default test ports and example port 853 usage; add direct evidence for default TCP/853 selection and per-primary override behavior. |
| ODS-FR-XOT-004 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` ALPN success and missing-ALPN tests; Knot XoT interop scripts with `openssl s_client -alpn dot` probes | Add retained release artifacts showing outbound ALPN `dot` in the OxideDNS handshake and abort behavior when a real peer does not confirm it. |
| ODS-FR-XOT-005 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` certificate-name, untrusted-certificate, expired-certificate, and trust-anchor tests; Knot XoT interop scripts with generated SAN certificates | Add retained certificate-validation evidence for malformed certificates and explicit SNI/SAN inspection in release artifacts. |
| ODS-FR-XOT-006 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` TLS-handshake failure and no-cleartext-retry test; XoT failure-path tests; Knot XoT interop scripts | Add code-review evidence that no Opportunistic Privacy Profile or cleartext fallback path exists, plus retained failure artifacts across certificate and TLS error classes. |
| ODS-FR-XOT-007 | MVP / Partial | `crates/oxidedns-core/src/config.rs` XoT client certificate/key config tests; `crates/oxidedns-server/src/lib.rs` mTLS success and missing-client-certificate tests; `config/oxidedns.example.toml` | Add retained interop evidence against a real primary requiring client certificates and explicit log/key-material redaction evidence for XoT client private keys. |
| ODS-FR-XOT-008 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` XoT+TSIG in-process test; `scripts/interop-knot-xot-tsig-docker.sh`; TSIG-only interop scripts | Current evidence covers AXFR with Knot and HMAC-SHA256 TSIG over XoT; add retained matrix coverage for XoT-only, TSIG-only, XoT+TSIG, and neither across supported transfer modes. |
| ODS-FR-XOT-009 | MVP / Not Verified | No accepted evidence yet | No current evidence demonstrates reuse of an established XoT TCP connection for successive transfers; decide whether to implement and test reuse or document no release claim for this optional behavior. |
| ODS-FR-XOT-010 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` TLS-handshake, certificate, ALPN, and mTLS failure tests; Knot XoT interop scripts | Add retained fault-injection artifacts covering TLS protocol errors and the full AXFR/IXFR abort and retry-state semantics required by the transfer state machine. |
| ODS-FR-XOT-011 | MVP / Partial | `crates/oxidedns-server/src/lib.rs` XoT TLS logging tests for session establishment, ALPN failure, and session close fields | Add retained release artifacts for successful and failed real-primary XoT sessions, plus explicit log-review evidence that certificate material, private keys, session keys, master secrets, and TLS key-derivation material are absent. |
| ODS-FR-XOT-012 | MVP / Partial | `scripts/audit-xot-revocation.sh`; `docs/operator-deployment-guide.md`; `docs/implementation-plan.md` | Static audit evidence covers no first-party CRL/OCSP revocation code, no OCSP/CRL or standalone HTTP/TLS client dependency in the locked set, and the current no-stapling-support posture. Add retained release artifacts and, if the underlying TLS client later exposes OCSP stapling, explicit stapled/non-stapled primary behavior evidence. |
| ODS-FR-EDNS-001..ODS-FR-EDNS-017 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; `crates/oxidedns-core/src/dns.rs` NSID and non-EDNS truncation tests; `scripts/interop-dnssec-serve.sh`; DNSSEC/RRL interop scripts where EDNS affects UDP sizing | Add retained release evidence for OPT placement, BADVERS, EDNS payload limits, DO handling, keepalive, padding, and broader truncation behavior. `ODS-FR-EDNS-015` now has focused unit coverage plus runtime fake-primary evidence for TC=1, response size <=512, and no response OPT on a non-EDNS query; `ODS-FR-EDNS-016..017` have unit coverage plus runtime fake-primary evidence for configured NSID on empty and non-empty NSID request data. |
| ODS-FR-TCP-001..ODS-FR-TCP-011 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; AXFR and query interop scripts | Add release artifacts for TCP framing, idle/read/write timeouts, connection limits, pipelining, truncation retry behavior, and graceful drain. |
| ODS-FR-DNSSEC-001 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-dnssec-nsec3-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/axfr.rs`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Retain per-type release artifacts for DNSKEY, RRSIG, NSEC, DS, NSEC3, and NSEC3PARAM parsing, storage, serving, and RRSIG type-covered matching. |
| ODS-FR-DNSSEC-002 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-dnssec-nsec3-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Expand DO=1/DO=0/no-OPT evidence across every DNSSEC response category and retain release logs. |
| ODS-FR-DNSSEC-003 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Add release evidence that RRSIGs are matched by type-covered field for answer, authority, and additional-section RRsets under normal and size-limited responses. |
| ODS-FR-DNSSEC-004 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-dnssec-nsec3-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Preserve NSEC and NSEC3 NXDOMAIN proof artifacts with query, response, and proof-record details suitable for release review. |
| ODS-FR-DNSSEC-005 | MVP / Partial | `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Broaden retained NODATA proof evidence beyond the current scripted/unit coverage, including both NSEC and NSEC3 cases. |
| ODS-FR-DNSSEC-006 | MVP / Partial | `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Promote wildcard denial-proof coverage from unit-level evidence to retained runtime evidence, including NSEC3 wildcard proof cases. |
| ODS-FR-DNSSEC-007 | MVP / Partial | `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Add retained runtime evidence for signed-child DS referrals and unsigned-child NSEC/NSEC3 no-DS referral proofs. |
| ODS-FR-DNSSEC-008 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-dnssec-nsec3-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Expand non-DO suppression artifacts to cover all response categories plus explicit DNSSEC QTYPE exceptions for RRSIG, NSEC, NSEC3, DNSKEY, DS, and NSEC3PARAM. |
| ODS-FR-DNSSEC-009 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-dnssec-nsec3-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Retain response OPT DO-bit evidence across positive, negative, referral, unsigned-zone, explicit-type, and truncation cases. |
| ODS-FR-DNSSEC-010 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-dnssec-nsec3-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Add release artifacts proving AD=0 across every authoritative response category, not only representative DNSSEC paths. |
| ODS-FR-DNSSEC-011 | MVP / Partial | `scripts/interop-dnssec-serve.sh`; `scripts/interop-dnssec-nsec3-serve.sh`; `scripts/interop-knot-dnssec-docker.sh`; `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Add release artifacts proving CD=0 across every authoritative response category, not only representative DNSSEC paths. |
| ODS-FR-DNSSEC-012 | MVP / Partial | `crates/oxidedns-core/src/dns.rs`; `docs/implementation-plan.md` | Add transfer-level evidence for DNSKEY, RRSIG, and DS records with synthetic reserved or private algorithm numbers; current evidence is direct-query/unit oriented. |
| ODS-FR-DNSSEC-013 | MVP / Partial | `scripts/audit-dnssec-passive.sh`; `scripts/check.sh`; `scripts/release-evidence-snapshot.sh`; `docs/implementation-plan.md`; `docs/operator-deployment-guide.md` | Static audit evidence covers transferred-RDATA parsing, stored RRset selection for DNSSEC augmentation, non-DO suppression, no first-party DNSSEC record construction, no DNSSEC signing/validation/key-management/rollover surface, and no locked DNSSEC signer/validator framework dependency. Add retained release artifacts and independent code-review evidence before acceptance. |
| ODS-FR-RR-001..ODS-FR-RR-007 | Alpha base; MVP expanded catalogue / Partial | `docs/OxideDNS-Secondary-SRS-v0.7.md` Appendix B; `docs/implementation-plan.md`; `scripts/check.sh` | Retain per-type parsing and serving evidence for the full RR catalogue, including compression policy and fixed-length/structured RDATA validation. |
| ODS-FR-ZONE-001..ODS-FR-ZONE-006 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; transfer interop scripts | Add explicit artifacts for in-memory indexing, atomic snapshot replacement, SOA serial/state handling, LOADING/ACTIVE/EXPIRED transitions, and query behavior by state. |
| ODS-FR-ZSM-001..ODS-FR-ZSM-013 | Alpha / Partial | `docs/implementation-plan.md`; `crates/oxidedns-core/src/config.rs` ZSM interval config tests; `crates/oxidedns-server/src/lib.rs` ZSM min/max interval, retry/expire, jitter, cooldown, deduplication, and concurrent-transfer tests; NOTIFY and IXFR interop scripts | Add retained release evidence for refresh scheduling, retry/expire timing, jitter, cooldown, deduplication, and concurrent transfer limits. |
| ODS-FR-RRL-001..ODS-FR-RRL-012 | MVP / Partial | `scripts/interop-rrl-udp.sh`; `docs/implementation-plan.md`; `docs/mvp-gap-register.md` | Finalize release threshold decisions and collect longer-running RRL evidence with counters, drops, slips, category buckets, and allowlist behavior mapped by requirement. |
| ODS-FR-COOKIE-001..ODS-FR-COOKIE-011 | MVP / Partial | `crates/oxidedns-core/src/dns.rs` DNS Cookie classification/tests; `crates/oxidedns-core/src/config.rs` cookie policy/rotation tests; `crates/oxidedns-server/src/lib.rs` UDP DNS Cookie/RRL/logging/metrics and secret-rotation tests; `scripts/interop-dns-cookie-dig.sh`; `config/oxidedns.example.toml`; `docs/mvp-gap-register.md` | RFC 9018 version-1 cookie construction/validation, option parsing, startup and optional rotated secrets with redacted fingerprint logging, disabled/lenient/strict policy config, strict BADCOOKIE response composition and debug logging, malformed length FORMERR handling, valid-cookie RRL exemption, global and per-source-prefix cookie-case counters, BADCOOKIE counters, bounded prefix cardinality, metrics exposition, and BIND `dig +cookie` runtime interop now have local evidence. Add broader BIND/Knot deployment interop and retained conformance evidence. |
| ODS-NEG-001..ODS-NEG-018 | Alpha / Partial | `docs/implementation-plan.md`; `scripts/check.sh`; protocol interop scripts where applicable | Separate implemented prohibitions from deferred/not-yet-applicable protocol paths before release review. |

## Non-Functional Requirements

| Requirement ID | Phase / status | Evidence pointer | Remaining acceptance gap |
| --- | --- | --- | --- |
| ODS-NFR-PERF-001..ODS-NFR-PERF-008 | MVP / Partial | `scripts/perf-smoke.sh`; `scripts/release-evidence-snapshot.sh`; `docs/mvp-gap-register.md` | Collect release benchmark artifacts for UDP/TCP throughput, direct-hit latency, p99 latency, AXFR/IXFR ingestion, startup timing, TSIG/DNSSEC performance, and overload behavior against the SRS v0.7 Reference Hardware/Profile mix. |
| ODS-NFR-REL-001..ODS-NFR-REL-007 | Alpha subset; MVP full / Partial | `docs/implementation-plan.md`; `docs/operator-deployment-guide.md`; `scripts/check.sh` | Retain shutdown/drain evidence, network-error resilience evidence, rolling-restart evidence, 30-day production-representative soak artifact, overload recovery, and clock-skew tolerance evidence. |
| ODS-NFR-SEC-001..ODS-NFR-SEC-007 | MVP / Partial | `fuzz/README.md`; `scripts/release-evidence-snapshot.sh`; `scripts/check.sh`; TSIG/XoT evidence scripts | Run and retain 24-hour fuzz campaigns per parser, dependency advisory/license/source review, secret redaction/zeroing evidence, unsafe-code inspection, and vulnerability disclosure/CVE policy evidence. |
| ODS-NFR-MAINT-001..ODS-NFR-MAINT-009 | MVP / Partial | `docs/implementation-plan.md`; `scripts/audit-safe-rust.sh`; `scripts/audit-maintainability.sh`; this matrix | Current line-count audit reports the first-party Rust source is over the ODS-NFR-MAINT-001 target; add architecture/release-note justification or refactor plan, requirement-to-module mapping, in-code requirement reference review, reproducible-build artifact, compatibility policy, coverage evidence, signed releases, and complete Operator Deployment Guide evidence. |
| ODS-NFR-PORT-001..ODS-NFR-PORT-005 | MVP / Partial | `docs/operator-deployment-guide.md`; `scripts/release-evidence-snapshot.sh`; `docs/mvp-gap-register.md` | Add Linux distribution/container evidence, architecture evidence, Kubernetes/container capability evidence, IPv4/IPv6 runtime evidence, and init-system independence evidence. |
| ODS-NFR-OBS-001..ODS-NFR-OBS-007 | Alpha subset; MVP full / Partial | `docs/implementation-plan.md`; `docs/operator-deployment-guide.md`; `scripts/perf-smoke.sh`; `crates/oxidedns-server/src/lib.rs` metrics tests; interop scripts checking metrics | Build-info metric exposition and query latency histogram bucket/count/sum behavior now have local test evidence. Add retained artifacts for structured log fields, log-level behavior, OpenMetrics compatibility, health state transitions, per-zone status metrics, build-info label accuracy, and histogram behavior under release traffic. |
| ODS-NFR-RES-001..ODS-NFR-RES-006 | MVP / Not Verified | `docs/mvp-gap-register.md`; `docs/implementation-plan.md` | Collect image size, per-record memory overhead, 10,000-zone/10-million-record capacity, file-descriptor rlimit, concurrent-transfer bound evidence, and idle-CPU evidence. |

## External Interface Requirements

| Requirement ID | Phase / status | Evidence pointer | Remaining acceptance gap |
| --- | --- | --- | --- |
| ODS-IF-NET-001..ODS-IF-NET-008 | Alpha / Partial | `docs/implementation-plan.md`; `docs/operator-deployment-guide.md`; `scripts/check.sh`; interop scripts; `crates/oxidedns-core/src/config.rs` notify-interface config and overlap tests; `crates/oxidedns-server/src/lib.rs` notify-interface runtime query/NOTIFY test; `crates/oxidedns-cli/tests/cli_modes.rs` bind-failure and notify-listener count tests | Add retained artifacts for broader multi-address bind behavior, bind failure exits beyond the local health-listener regression, outbound source behavior, and IPv4/IPv6 interface coverage. |
| ODS-IF-CONF-001..ODS-IF-CONF-012 | Alpha / Partial | `config/oxidedns.example.toml`; `docs/operator-deployment-guide.md`; `docs/implementation-plan.md`; `scripts/check.sh`; `crates/oxidedns-core/src/config.rs` NSID config, notify-interface config, obsolete `interfaces.xot` rejection, TSIG fudge config, transfer-ingest-cap config, suspicious-warning subset, and redacted TOML dump tests; `crates/oxidedns-cli/src/main.rs` `ODS_<SECTION>_<KEY>` override and warning tests including `ODS_TSIG_FUDGE_SECONDS` and `ODS_LIMITS_MAX_TRANSFER_INGEST_BYTES`; `crates/oxidedns-cli/tests/cli_modes.rs` `--validate-config`, `--dump-config`, env-override, unrecognised-`ODS_*`, and suspicious-configuration warning tests; `crates/oxidedns-cli/tests/signal.rs` no-SIGHUP-reload behavioral coverage; `crates/oxidedns-server/src/lib.rs` configuration-warning metric, XoT trust-anchor expiry warning, SOA-timer near-ZSM-ceiling warning, configured TSIG-fudge, and transfer-ingest-cap runtime tests | Add broader schema/backward-compatibility evidence and broader secret/TLS file-mode evidence. |
| ODS-IF-LOG-001..ODS-IF-LOG-008 | Alpha / Partial | `docs/operator-deployment-guide.md`; `docs/implementation-plan.md`; `scripts/check.sh` | Add retained stdout/stderr split, JSON/logfmt, log-level, canonical field-name, bootstrap logging, log-size-limit, and lazy-formatting artifacts. |
| ODS-IF-HEALTH-001..ODS-IF-HEALTH-006 | Alpha subset; MVP full / Partial | `docs/operator-deployment-guide.md`; `docs/implementation-plan.md`; `crates/oxidedns-server/src/lib.rs` health endpoint tests; `scripts/perf-smoke.sh`; interop scripts waiting on `/readyz` and checking `/metrics` | `/livez`, JSON `/readyz`, `/healthz` readiness alias, JSON 404s, method rejection, disabled endpoint behavior, focused 100 ms probe checks, gzip-capable `/metrics` responses, and per-source `/metrics` rate limiting now have local evidence. Add retained artifacts for unauthenticated access boundary, isolation from DNS query latency, broader state-transition timing, and production-representative rate-limit behavior. |
| ODS-IF-SIG-001..ODS-IF-SIG-004 | Alpha / Partial | `docs/implementation-plan.md`; `docs/operator-deployment-guide.md`; `scripts/check.sh`; `crates/oxidedns-cli/tests/signal.rs`; `crates/oxidedns-server/src/process_signals.rs`; `crates/oxidedns-cli/src/main.rs` | Binary tests cover SIGTERM/SIGINT graceful exit, SIGHUP behavioral ignore, Linux `SigIgn` disposition evidence for SIGHUP and SIGPIPE, and Linux `SigCgt` evidence that SIGHUP, SIGPIPE, SIGQUIT, SIGUSR1, and SIGUSR2 have no installed handlers. Add release-retained signal artifacts and stdout/stderr broken-pipe runtime evidence. |
| ODS-IF-PROC-001..ODS-IF-PROC-004 | Alpha subset; MVP full / Partial | `crates/oxidedns-cli/src/main.rs`; `crates/oxidedns-cli/tests/cli_modes.rs`; `docs/implementation-plan.md`; `config/oxidedns.example.toml`; `scripts/check.sh` | Config-invalid, config-parse/read, usage, listen-socket bind-failure, XoT TLS-file read failure, OS-startup mapping, `--version`/`-V`, `--help`/`-h`, and `--example-config` paths now have local tests. Add broader sysexits coverage for less common runtime classes and release-retained version/help/example-config output artifacts. |

## Verification Requirements

| Requirement ID | Phase / status | Evidence pointer | Remaining acceptance gap |
| --- | --- | --- | --- |
| ODS-VER-001 | MVP / Partial | `docs/verification-ledger.md`; this matrix | Map every SRS section 3 through 6 requirement to accepted verification method evidence, not only family-level evidence. |
| ODS-VER-002 | MVP / Partial | `scripts/release-evidence-snapshot.sh`; `docs/verification-ledger.md` | Ensure CI/release retention captures logs, benchmark results, code-review records, fuzz summaries, and interop outputs for each release. |
| ODS-VER-003..ODS-VER-004 | Alpha / Partial | BIND, NSD, and Knot interop scripts listed in `docs/verification-ledger.md` | Retain release-review outputs and expand representative-zone matrix where gaps remain. |
| ODS-VER-005..ODS-VER-006 | MVP / Partial | `docs/OxideDNS-Secondary-SRS-v0.7.md` Appendix A | Keep RFC clause mapping synchronized with requirement verification status and out-of-scope rationale. |
| ODS-VER-007 | Alpha / Partial | `docs/verification-ledger.md`; interop scripts listed there | Alpha evidence exists as a working scaffold; release review must decide which partial rows are acceptable for Alpha signoff. |
| ODS-VER-008 | MVP / Deferred | `docs/mvp-gap-register.md`; `docs/verification-ledger.md` | Complete all SRS acceptance protocol, non-functional, interop, operator-doc, and retained-evidence gaps before asserting ODS-VER-008. |
| ODS-VER-009 | MVP / Partial | `docs/verification-ledger.md`; this matrix | Expand this first-pass family matrix into the canonical per-requirement status table with date and evidence reference for every SRS section 3 through 6 requirement. |
| ODS-VER-010..ODS-VER-015 | MVP / Not Verified | `docs/OxideDNS-Secondary-SRS-v0.7.md`; `docs/mvp-gap-register.md` | Add release-notes verification gate, Continuous/Periodic/Gate Test Plan cadence mapping, regression detection/triage policy, recorded primary versions/configurations, RFC compliance assertion publication, and verification responsibility allocation. |
