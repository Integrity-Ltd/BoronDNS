# Engineering MVP and SRS Verification Ledger

This ledger is the lightweight working record for evidence against the SRS
verification requirements, especially ODS-VER-002 and ODS-VER-009. It is not a
complete per-requirement traceability matrix yet. The first slice groups related
requirements at a high level so Engineering MVP and SRS acceptance progress has
one maintainable place to accumulate evidence while implementation is still
moving.

The SRS-defined MVP in ODS-VER-008 is treated here as a full acceptance gate,
not as the near-term engineering milestone. Rows with target `MVP` refer to
that SRS acceptance gate unless a note explicitly says otherwise.

## Status Values

- **Not Verified**: no accepted evidence is recorded in this ledger.
- **Partial**: evidence exists, but does not yet cover the full listed scope.
- **Verified**: evidence covers the listed scope and is suitable for release
  review.
- **Deferred**: verification is intentionally targeted at a later milestone.

## Ledger

| Area | Target | Requirement Coverage | Evidence State | Evidence Pointers | Notes |
| --- | --- | --- | --- | --- | --- |
| Architectural invariants | Alpha | ODS-INV-001..ODS-INV-009 | Partial | docs/implementation-plan.md; scripts/check.sh; scripts/audit-invariants.sh; scripts/audit-readonly-runtime.sh; `crates/oxidedns-core/src/dns.rs` tests `dns_update_opcode_gets_notimp_without_zone_mutation`, `concurrent_snapshot_replacement_answers_from_one_zone_version`, and `answer_datagram_does_not_panic_for_malformed_corpus` | `scripts/audit-invariants.sh` now records repeatable static inspection evidence for all SRS v0.7 invariants: secondary-only scope, memory-resident query path, atomic snapshot publication, no persistent operational writes, static configuration/control surface, first-party safe Rust, authoritative-only response composition, single-process architecture, and no runtime code loading. `scripts/audit-readonly-runtime.sh` adds runtime evidence with non-writable `TMPDIR`, transfer, readiness, query serving, `/proc` child-process and thread-count inspection, retained artifacts, and optional strace write-intent checking where available. `dns_update_opcode_gets_notimp_without_zone_mutation` covers DNS UPDATE rejection without zone mutation. `concurrent_snapshot_replacement_answers_from_one_zone_version` stress-checks query responses during atomic snapshot replacement so CNAME chains cannot mix old and new zone versions. `answer_datagram_does_not_panic_for_malformed_corpus` adds focused malformed-input panic-free query-path evidence. Container-level read-only root filesystem artifacts and broader retained fuzz/panic-free artifacts remain acceptance gaps. |
| Core authoritative query behavior | Alpha | ODS-FR-CORE-001..ODS-FR-CORE-028; ODS-FR-QRY-001..ODS-FR-QRY-024; ODS-FR-NRESP-001..ODS-FR-NRESP-006; ODS-FR-URR-001..ODS-FR-URR-009; ODS-FR-RR-001..ODS-FR-RR-007; ODS-FR-ZONE-001..ODS-FR-ZONE-006 | Partial | docs/implementation-plan.md; scripts/check.sh; `crates/oxidedns-core/src/dns.rs` tests `response_owner_names_are_compressed_against_question`, `compressed_qname_is_reencoded_before_response_compression`, `configured_cname_chain_limit_returns_authoritative_servfail`, `cname_loop_returns_authoritative_servfail_with_partial_chain`, `permitted_name_rdata_is_compressed_but_unknown_rdata_stays_opaque`, and `unknown_type_query_preserves_zero_and_pointer_like_rdata`; `crates/oxidedns-server/src/lib.rs` query metrics and CNAME warning-field tests | Alpha excludes ODS-FR-QRY-003..ODS-FR-QRY-007 only where ODS-VER-007 says minimal-ANY is deferred. Local evidence now covers response owner-name compression, safe re-encoding of compressed request QNAMEs, CNAME loop/limit SERVFAIL composition and warning fields, compression of currently encoded permitted name RDATA, opaque preservation of unknown pointer-like RDATA, and global/per-zone query RCODE counters; retained release artifacts and broader per-requirement split evidence remain open. |
| AXFR and outbound response validation | Alpha | ODS-FR-SPOOF-001..ODS-FR-SPOOF-007; ODS-FR-AXFR-001..ODS-FR-AXFR-024 | Partial | docs/implementation-plan.md; scripts/interop-bind-axfr.sh; `crates/oxidedns-server/src/lib.rs` AXFR ingestion-size cap test | BIND interop is represented. AXFR cumulative DNS transfer message payload cap has focused runtime evidence. NSD/Knot evidence and retained transfer-size artifacts remain an MVP expansion unless Alpha chooses a different primary. |
| TCP, EDNS, NOTIFY, and zone state machine | Alpha | ODS-FR-TCP-001..ODS-FR-TCP-011; ODS-FR-EDNS-001..ODS-FR-EDNS-017; ODS-FR-NOTIFY-001..ODS-FR-NOTIFY-011; ODS-FR-ZSM-001..ODS-FR-ZSM-013 | Partial | docs/implementation-plan.md; scripts/check.sh; `crates/oxidedns-core/src/dns.rs` NSID and non-EDNS truncation tests; `crates/oxidedns-core/src/config.rs` ZSM max-interval and LOADING-warning-threshold config tests; `crates/oxidedns-server/src/lib.rs` ZSM interval-clamping, LOADING-duration metric, and repeated long-LOADING warning tests; scripts/interop-dnssec-serve.sh; scripts/interop-tcp-truncation-retry.sh; scripts/interop-bind-notify-refresh.sh; scripts/interop-nsd-notify-refresh-docker.sh; scripts/interop-knot-notify-refresh-docker.sh | BIND, NSD, and Knot NOTIFY refresh interop now cover real primary update paths. Non-EDNS 512-octet truncation/no-OPT behavior covers `ODS-FR-EDNS-015` at unit level and in the fake-primary runtime harness, and `scripts/interop-tcp-truncation-retry.sh` retains runtime evidence that the same large answer is truncated over UDP but complete over TCP. Configured NSID response behavior covers `ODS-FR-EDNS-016..017` at unit level and in the fake-primary runtime harness for empty and non-empty NSID request data. ZSM REFRESH/RETRY scheduling now enforces the configured minimum and maximum effective interval while preserving original SOA timer values, and `ODS-FR-ZSM-013` has local metric plus repeated structured warning evidence; retained release artifacts and broader TCP/NOTIFY/ZSM split evidence remain before Alpha signoff. |
| TSIG Alpha subset | Alpha | ODS-FR-TSIG-001; ODS-FR-TSIG-005..ODS-FR-TSIG-012; ODS-FR-TSIG-017; ODS-NEG-013; ODS-NEG-014 | Partial | docs/implementation-plan.md; scripts/interop-bind-tsig-axfr.sh; scripts/interop-nsd-tsig-axfr-docker.sh; scripts/interop-knot-tsig-axfr-docker.sh; `crates/oxidedns-server/src/lib.rs` ordinary-query TSIG tests | ODS-VER-007 requires HMAC-SHA256 interop with at least one TSIG-configured primary; current evidence covers BIND, NSD, and Knot AXFR TSIG. Local tests now cover ordinary query TSIG key lookup, unknown-key BADKEY response, BADSIG handling, response signing, and UDP RRL bypass for authenticated queries, while full TSIG remains MVP scope. |
| Interfaces, shutdown, and observability | Alpha subset; MVP full | ODS-IF-NET-001..ODS-IF-NET-008; ODS-IF-CONF-001..ODS-IF-CONF-012; ODS-IF-LOG-001..ODS-IF-LOG-008; ODS-IF-HEALTH-001..ODS-IF-HEALTH-006; ODS-IF-SIG-001..ODS-IF-SIG-004; ODS-IF-PROC-001..ODS-IF-PROC-003; ODS-IF-PROC-004 when implemented; ODS-NFR-REL-001..ODS-NFR-REL-007; ODS-NFR-MAINT-001; ODS-NFR-MAINT-003; ODS-NFR-PORT-001..ODS-NFR-PORT-004; ODS-NFR-OBS-001; ODS-NFR-OBS-002; ODS-NFR-OBS-004; ODS-NFR-OBS-005; ODS-NFR-OBS-007; ODS-NFR-RES-001 | Partial | docs/implementation-plan.md; scripts/check.sh; scripts/capture-cli-evidence.sh; scripts/capture-log-evidence.sh; scripts/audit-log-fields.py; scripts/audit-log-lazy-formatting.py; scripts/capture-signal-evidence.sh; scripts/capture-health-metrics-evidence.sh; `crates/oxidedns-server/src/lib.rs` health endpoint, SRS-named per-zone status/query/query-RCODE metrics, long-LOADING warning, XoT trust-anchor expiry warning, SOA-timer warning, notify-interface runtime, TSIG-fudge, transfer-ingest-cap runtime tests, and configured latency-histogram tests; `crates/oxidedns-server/src/process_signals.rs`; `crates/oxidedns-cli/src/main.rs`; `crates/oxidedns-cli/tests/cli_modes.rs`; `crates/oxidedns-cli/tests/signal.rs`; `crates/oxidedns-core/src/config.rs` notify-interface config, redacted dump, TSIG config, metrics config, and transfer-ingest-cap config tests | SRS v0.7 Alpha adds `ODS-IF-CONF-008..012`, `ODS-IF-LOG-005..008`, `ODS-IF-PROC-001..003`, and `ODS-NFR-OBS-005`; ODS-IF-PROC-004 is MAY-level and is tracked only because it is implemented. `--validate-config`, redacted effective `--dump-config`, valid optional `--example-config` output, optional `[interfaces].notify` UDP/TCP listener config with DNS-listener overlap rejection and runtime query/NOTIFY evidence, obsolete `interfaces.xot` rejection, SRS-shaped `--version`/`-V` and `--help`/`-h`, scalar server/health/limits/TSIG `ODS_<SECTION>_<KEY>` env override tests, unrecognised-`ODS_*` and suspicious-configuration warning tests including large TSIG fudge, low transfer-ingest caps, XoT trust-anchor expiry within 30 days, and transferred SOA REFRESH/RETRY values approaching `[limits].zsm_max_interval_secs`, config/usage/UDP/TCP/health listen-bind/XoT-file exit-code tests, unit-level RuntimeError and TransferError exit-code mapping tests, JSON/logfmt logging selection and truncation tests, static canonical log-field audit for runtime peer/error/category fields, static lazy-formatting audit for first-party `debug!`/`trace!` emission sites, representative release-retainable JSON/logfmt runtime stream capture, bounded logfmt runtime truncation capture, `/livez`, JSON `/readyz`, `/healthz` readiness alias, JSON 404s, focused 100 ms health probe checks plus representative retained timing artifacts, gzip-capable and per-source-IP rate-limited `/metrics` with retained headers/body artifacts including an over-limit scrape-burst summary, SRS-named per-zone state, SOA serial, last/next refresh, refresh-failure, per-zone query and query-RCODE metrics, long-LOADING structured warning fields, `oxidedns_secondary_configuration_warnings_total`, build-info gauge, query latency histogram with operator-configurable bucket boundaries, SIGTERM/SIGINT graceful-exit tests plus retained artifacts, SIGHUP behavioral-ignore test plus retained artifact, stdout/stderr broken-pipe survival test plus retained artifact, Linux SIGHUP/SIGPIPE `SigIgn` evidence plus retained proc-status artifact, Linux no-extra-handler `SigCgt` evidence plus retained artifact, and release-retained version/help/example-config output capture now provide partial interface/observability evidence. Longer production-representative metrics rate-limit traffic, release evidence for build-info labels and histogram behavior under release traffic, profiling artifacts under `info` verbosity, and reliability extensions remain MVP/deferred evidence. |
| Negative requirements | Alpha | ODS-NEG-001..ODS-NEG-018 | Partial | docs/implementation-plan.md; scripts/check.sh | Some negative requirements overlap deferred MVP features; release review must separate implemented prohibitions from not-yet-applicable protocol paths. |
| Alpha interop gate | Alpha | ODS-VER-003; ODS-VER-004; ODS-VER-007 | Partial | scripts/engineering-mvp-evidence.sh; scripts/interop-version-evidence.sh; scripts/interop-bind-axfr.sh; scripts/interop-bind-tsig-axfr.sh; scripts/interop-bind-notify-refresh.sh; scripts/interop-nsd-axfr-docker.sh; scripts/interop-nsd-tsig-axfr-docker.sh; scripts/interop-nsd-notify-refresh-docker.sh; scripts/interop-knot-axfr-docker.sh; scripts/interop-knot-tsig-axfr-docker.sh; scripts/interop-knot-notify-refresh-docker.sh; scripts/interop-ixfr-notimp-fallback.sh | Current scripts cover AXFR, HMAC-SHA256 TSIG AXFR, and NOTIFY refresh with BIND, NSD, and Knot. Real-primary scripts record `primary-version.txt` evidence for `ODS-VER-013`, binding each pass/fail assertion to the tested primary version, OS/package context, configuration profile, transport, and security mode. `scripts/engineering-mvp-evidence.sh` captures the narrow Engineering MVP evidence profile against BIND. Alpha requires at least one of NSD, Knot DNS, or BIND 9; MVP still needs IXFR and remaining deferred protocol evidence. |
| Deferred protocol families | MVP | ODS-FR-IXFR-001..ODS-FR-IXFR-019; ODS-FR-XOT-001..ODS-FR-XOT-012; ODS-FR-DNSSEC-001..ODS-FR-DNSSEC-013; ODS-FR-RRL-001..ODS-FR-RRL-012; ODS-FR-COOKIE-001..ODS-FR-COOKIE-011 | Partial | docs/implementation-plan.md; scripts/interop-version-evidence.sh; scripts/interop-bind-ixfr-refresh.sh; scripts/interop-knot-ixfr-refresh-docker.sh; scripts/interop-ixfr-notimp-fallback.sh; scripts/interop-knot-xot-docker.sh; scripts/interop-knot-xot-tsig-docker.sh; scripts/interop-knot-dnssec-docker.sh; scripts/interop-rrl-udp.sh; scripts/interop-dns-cookie-dig.sh; scripts/interop-dnssec-serve.sh; scripts/interop-dnssec-nsec3-serve.sh; scripts/audit-xot-revocation.sh; scripts/audit-dnssec-passive.sh; config/oxidedns.example.toml; scripts/check.sh; `crates/oxidedns-core/src/dns.rs` DNS Cookie tests; `crates/oxidedns-core/src/config.rs` cookie policy and rotation config tests; `crates/oxidedns-server/src/lib.rs` UDP DNS Cookie/RRL/logging/metrics, TSIG query RRL bypass, secret-rotation, and XoT TLS logging tests | IXFR has parser, true incremental BIND and Knot refresh evidence, and fallback evidence; real-primary IXFR, XoT, and DNSSEC interop scripts record primary-version artifacts for reproducibility. DNSSEC has unit foundations plus runtime fake-primary DO-sensitive RRSIG/NSEC/NSEC3/DNSKEY/NSEC3PARAM, Knot signed-primary NSEC3 evidence, and passive no-signing/no-validation/no-key-rollover audit evidence; RRL has unit and runtime UDP drop/slip evidence across all response categories plus local TSIG-authenticated query exemption coverage; and XoT now has configuration, startup TLS-file validation, in-process TLS transport, XoT+TSIG, mTLS client-certificate, certificate-name, untrusted-cert, expired-cert, ALPN-failure, missing-client-cert, structured TLS establishment/ALPN-failure/session-close log evidence, no-real-time-revocation static audit evidence, and Knot XoT plus XoT+TSIG interop evidence. DNS Cookies now have RFC 9018 construction/validation, disabled/lenient/strict policy, optional in-process rotation, strict BADCOOKIE, RRL-exemption, startup/rotation/BADCOOKIE logging, bounded global/per-source-prefix metrics, and BIND `dig +cookie` runtime interop evidence; broader deployment interop artifacts and retained release evidence remain open. |
| SRS acceptance non-functional gates | MVP | ODS-NFR-PERF-001..ODS-NFR-PERF-008; ODS-NFR-SEC-001..ODS-NFR-SEC-007; ODS-NFR-MAINT-001..ODS-NFR-MAINT-009; ODS-NFR-PORT-001..ODS-NFR-PORT-005; ODS-NFR-OBS-001..ODS-NFR-OBS-007; ODS-NFR-RES-001..ODS-NFR-RES-006 | Partial | docs/implementation-plan.md; fuzz/README.md; SECURITY.md; docs/architecture.md; scripts/check-security-policy.sh; scripts/perf-smoke.sh; scripts/check-perf-regression.py; scripts/release-evidence-snapshot.sh; scripts/audit-safe-rust.sh; scripts/audit-maintainability.sh; `crates/oxidedns-server/src/lib.rs` metrics tests | Parser fuzz targets now cover DNS datagrams, AXFR/IXFR transfer streams, TSIG message paths, and NOTIFY/EDNS datagrams; `scripts/perf-smoke.sh` adds repeatable startup, AXFR ingestion, metrics, UDP direct-hit latency smoke evidence, retained smoke metrics, and optional raw metrics artifacts proving build-info plus non-zero UDP direct-hit histogram counts; `scripts/check-perf-regression.py` compares retained smoke metrics against a rolling history when supplied; `scripts/check-security-policy.sh` checks the vulnerability disclosure, CVE handling, release-signing, and role-allocation policy evidence; `scripts/audit-safe-rust.sh` records first-party safe-Rust lint/scan evidence; `scripts/audit-maintainability.sh` records source line-count and module-map evidence; `scripts/release-evidence-snapshot.sh` captures release-review command logs, tool versions, git state, fuzz compile checks, cargo-deny output, security-policy output, safe-Rust audit output, maintainability audit output, performance smoke output, and perf-smoke raw metric artifacts. SRS v0.7 acceptance still needs TCP/TSIG/DNSSEC performance targets, overload and clock-skew reliability, release-specific policy review records, coverage/operator-guide gates, actual signed-release artifacts, broader histogram behavior evidence under production-representative traffic, and idle-CPU resource evidence. |
| SRS acceptance and traceability | MVP | ODS-VER-001..ODS-VER-015 | Partial | docs/OxideDNS-Secondary-SRS-v0.7.md; docs/verification-ledger.md; docs/appendix-a-traceability-matrix.md; docs/test-plan.md; docs/architecture.md; scripts/check-test-plan.sh; scripts/check-security-policy.sh; scripts/check-perf-regression.py; docs/release-notes-template.md; scripts/check-release-notes.sh; scripts/interop-version-evidence.sh; scripts/evidence-artifacts.sh | The Appendix A matrix is now a first-pass family-level traceability artifact. The Test Plan records cadence mapping and regression policy for `ODS-VER-011..012`, and smoke-metric regression comparison exists when a history is supplied; interop scripts produce primary-version/configuration artifacts for `ODS-VER-013`; evidence snapshot scripts retain them under `interop-primary-versions/`; the release-notes checker enforces the required publication sections; and `docs/architecture.md` allocates the `ODS-VER-015` verification responsibilities for the current MVP scaffold. Remaining work is hosted CI/scheduler enactment, populated release notes, persisted full benchmark baselines, completed RFC assertion publication content, and release-specific responsibility sign-off before claiming acceptance. |

## Maintenance Check

Run:

```sh
python3 scripts/check-verification-ledger.py
```

The check validates that requirement IDs and same-prefix ranges referenced by
this ledger exist in `docs/OxideDNS-Secondary-SRS-v0.7.md`. It also validates the
ledger status values in the table above. This catches typo-level drift without
pretending to prove requirement satisfaction.
